# HW4 Deliverable Staging

This folder mirrors the required zip-root structure while development is in
progress.

Expected final zip contents:

```text
kernels/student_kernel.cu
Team_<Team Number>_HW4_report.pdf
```

Current staged implementation:

```text
kernels/student_kernel.cu
```

Add the final report PDF here before creating `Team_<Team Number>_HW4.zip`.
